server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes (/home, /profile, ...) resolve to
    # index.html so React Router can handle them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
